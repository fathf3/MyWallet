
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div id="calendar"></div>
        </div>
    </div>
</div>
<script>
  


    document.addEventListener('DOMContentLoaded', function () {
        var calendarEl = document.getElementById('calendar');
        var calendar = new FullCalendar.Calendar(calendarEl, {
            slotMinTime: '08:00:00', // Başlangıç saati
            slotMaxTime: '23:00:00', // Bitiş saati
            hiddenDays: [0, 6],
            initialView: 'dayGridMonth',
            timeZone: 'local',
            locale: 'tr',
            events: {
                url: '/Seans/GetSessions',
                method: 'GET',
                extraParams: {
                    custom_param1: 'something',
                    custom_param2: 'somethingelse'
                },
                failure: function () {
                    alert('there was an error while fetching events!');
                },
                color: 'yellow',   // a non-ajax option
                textColor: 'black', // a non-ajax option
                
                
            },
            headerToolbar: {
                            left: 'prev,next today',
                            center: 'title',
                            right: 'dayGridMonth,timeGridWeek,timeGridDay',
                minTime: "08:00:00",
                maxTime: "22:00:00",
                        },
        
        });
        calendar.render();
    });


    // document.addEventListener('DOMContentLoaded', function ()
    // {
    //     var calendarEl = document.getElementById('calendar');

    //     // Razor modelden JSON'a dönüştürülmüş oturum verileri


    //     var calendar = new FullCalendar.Calendar(calendarEl, {
    //         initialView: 'dayGridMonth',

    //         events: [{
    //             title : title,
    //             start: startDate,
    //             end : endDate

    //         }],  // Oturumları doğrudan takvime ekliyoruz
    //         headerToolbar: {
    //             left: 'prev,next today',
    //             center: 'title',
    //             right: 'dayGridMonth,timeGridWeek,timeGridDay'
    //         }
    //     });

    //     calendar.render();
    // });
</script>

@section Scripts {
    <script src="~/js/fullcalendarv5.min.js"></script>
}